# MongoDB connection string (local install or MongoDB Atlas)
# Example Atlas: mongodb+srv://<user>:<password>@cluster.mongodb.net/crm_db
MONGO_URI=mongodb://localhost:27017/crm_db

# Secret key for JWT signing — use a long random string in production
JWT_SECRET=your_jwt_secret_key_here

# Express server port
PORT=5000
